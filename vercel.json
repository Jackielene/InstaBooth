{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "buildCommand": "cd photobooth && npm install && npm run build",
  "outputDirectory": "photobooth/dist",
  "installCommand": "npm install"
} 